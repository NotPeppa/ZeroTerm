# ZeroTerm 代码审计报告

- **日期**:2026-07-26
- **版本**:v0.1.11(main 分支)
- **范围**:Rust 核心(crypto / vault / store / sync / ssh / app / ffi)、Tauri 桌面后端、桌面前端(main.js / redact.js)、Android 客户端、供应链与 CI/CD
- **方法**:6 个分区并行深审(逐文件读码 + 跨 crate 数据流追踪 + 关键路径抽样复现)+ 依赖公告核对。所有发现均经代码上下文验证,标注 `文件:行号`;并对若干最高危结论做了独立回读复核。

> ⚠️ 本报告聚焦**问题**。多数模块的底层构造(密码学原语、SQL 参数化、CSP、路径遍历防护、Android 基线加固)经核实是扎实的,详见文末「已验证稳健的部分」。

---

## 修复进度(第三轮 · 延后项收尾与实时依赖复核,2026-07-27)

> 本节是当前权威状态。下方“第二轮”及原始问题详情保留为历史快照;其中标为“延后”的条目若与本节冲突,以本节为准。

此前状态面板中的 6 个收尾任务现已全部完成:

| 任务 | 当前状态 | 第三轮落地与验证 |
|---|---|---|
| **DEP-1** | ✅ 已修 | 最初尝试的 russh 0.54.5 虽修复旧的 channel-window 溢出,但实时 RustSec 库又命中 2026 年新公告。现继续升级到 **russh 0.62.4 / russh-cryptovec 0.62.0 / russh-sftp 2.3.0**,并适配 agent 证书身份、显式 channel-open 确认等 API 变化。 |
| **SSH-4** | ✅ 已修 | session handle 改为共享异步互斥,global request 与 channel open 可安全并存,不再依赖 `Arc::get_mut` 或调用顺序。 |
| **SSH-5** | ✅ 已修 | 每个 session 建立单一 `forwarded-tcpip` dispatcher,按 `(connected_address, connected_port)` 精确分派至各 `-R` 订阅;重复路由拒绝、取消时清理并 best-effort cancel server listener。 |
| **SSH-7** | ✅ 已修 | 保存主机的 interactive PTY、exec 与 SFTP 三个 FFI 入口均解析并持有一跳 ProxyJump 会话;跳板生命周期覆盖目标 session。 |
| **CORE-4** | ✅ 已修 | 记录 AAD v3 绑定 `record_id + version + kind + deleted`;兼容读取 v1/v2 活记录与旧空墓碑,但拒绝把旧活密文篡改为 deleted。列表、查询、删除、远端应用等路径均先认证元数据再筛选。 |
| **CORE-7** | ✅ 已修 | verifier 使用独立 HKDF 域派生密钥,并兼容检测旧 verifier 后迁移。 |

本轮同时完成:

- **DEP-2 / DEP-3** ✅(有记录的例外):russh 0.62.4 改用 rsa 0.10.0-rc.18,但 Marvin 公告仍覆盖且 RustSec 明确无可用修复。ZeroTerm 不暴露该公告所需的 RSA 私钥解密预言机,故两份 audit 配置保留唯一的威胁模型例外 `RUSTSEC-2023-0071`;CI audit job 已去掉 `continue-on-error`,除这一无补丁例外外的漏洞均硬阻断。
- **2026-06 新公告** ✅:`quick-xml` 升至 0.41.0、`quinn-proto` 升至 0.11.16;core 与 desktop 两份锁文件 `cargo audit` 均为 **0 unignored vulnerabilities**。
- **TAURI-6** ✅:桌面后端 94 处 `Mutex::lock().unwrap()` 改为锁中毒恢复,避免一次 panic 使子系统永久连锁崩溃。
- **TAURI-7 / FE-5** ✅:vault 未解锁时拒绝持久化 AI 历史,不再退化成 `0600` 明文;历史明文仍可读取,解锁后会升级为 vault 加密 blob。
- **SSH-3** ✅:`AuthMethod` 与 `HostAuth` 保持既有序列化/API 形状,新增确定性 `Drop` 清零密码、私钥 PEM 与 passphrase;`Debug` 脱敏继续保留。
- **SSH-2** ✅:固定 vendor `russh 0.62.4` 并以最小补丁让客户端 KEX 区分普通公钥与 OpenSSH 主机证书、把原始证书 blob 纳入交换哈希、用证书主体密钥验证 KEX 签名,再通过默认拒绝的独立 handler 回调交给 ZeroTerm。客户端协商证书 host-key 算法;known_hosts 完整处理哈希 hostname、通配/否定模式、`@revoked` 与 `@cert-authority`,并校验证书 CA 签名、有效期、host 类型、principal 与 critical options。Interactive 不对未知证书做叶子 TOFU,避免绕过 CA 模型。
- **SYNC-8 降级防护** ✅:设备一旦验证或写入过 MAC manifest,就在本地 sync state 持久化“MAC required”;此后缺失 MAC 为硬错误。`meta_version > 0` 的无 MAC manifest 对新设备同样拒绝,并有“同时清空 MAC/版本仍被已有设备拒绝”的回归覆盖。
- **SYNC-8 keyring 认证与设备撤销** ✅:keyring 条目集合增加根密钥派生 MAC,manifest 绑定不可变 keyring 路径与单调 `root_epoch`;撤销设备时必须提供新密码,后端硬性拒绝任何仍能解包旧 keyring 条目的旧密码,随后生成新根密钥、直接从已收敛且 clean 的本地 vault 构造新代完整活记录/墓碑快照(不会在撤销前额外发布一份“当前数据+旧根密钥”快照)、为保留设备重包根密钥,最后原子切换 manifest。事件 envelope 与快照也按 root epoch 做 MAC,因此持有旧根密钥的被撤设备不能伪造新一代 delete 或篡改墓碑。桌面设备列表已提供“撤销”入口并同步更新系统钥匙串密码。
- **TAURI-10** ✅:system metrics 与 Docker 远端命令改借用现有 host SSH/SFTP pool 的共享 `Arc<Session>`,轮询不再逐次新建 SSH/ProxyJump 连接。
- **真实 SSH E2E** ✅:新增 opt-in `live_sshd` 集成测试,已对本机 OpenSSH 10.2 实测通过密钥握手、exec、SFTP、local forward(`-L`)、SOCKS5 dynamic forward(`-D`)、两路并发 remote forward(`-R`)精确路由、SSH-over-SSH ProxyJump;并以 `HostCertificate` 启动临时真实 sshd、known_hosts 仅放 `@cert-authority` 的 Strict 策略再次完整通过,证明不是退回普通叶子密钥信任。

验证结果:

- core workspace(含 WebDAV/S3 feature):**226 passed,0 failed**,另 1 个 macOS 系统钥匙串 miss 用例因本机 keychain 调用长期阻塞而在整套回归中单独跳过;该用例与 SSH/依赖升级无关。
- SYNC-8 根密钥轮换专项:**5 项新增认证/撤销测试通过**(keyring 集合拼接、事件/快照 MAC、旧设备/旧密码拒绝、墓碑保留、伪造删除拒绝),并包含两设备→撤销→新设备加入的真实文件仓库回归。
- 真实 OpenSSH 主机证书 Strict E2E:**1 passed,0 failed**。
- core `clippy --no-deps --all-targets -D warnings`:通过。
- desktop backend:**42 passed,0 failed**。
- frontend:`node --check` 通过,脱敏回归 **28 passed,0 failed**。
- Android:arm64-v8a + x86_64 Rust/FFI 构建及 `:app:compileDebugKotlin` 通过。
- `cargo audit`:core 与 desktop 均 **0 unignored vulnerabilities**;唯一 ignored vulnerability 为上述无补丁、无对应解密接口的 RSA Marvin 公告。另有若干 unmaintained/unsound/yanked **warning**(主要来自 vendored terminal/Tauri GTK 传递依赖及 `anyhow`),不属于当前 cargo-audit 的 vulnerability 失败项,需持续跟踪。

原始审计代码项已全部关闭。仍需持续治理的是供应链 warning;若要求“任何 warning 都阻断”,需另行升级 Tauri/GTK/vendored terminal 依赖并将 CI 改为 `cargo audit --deny warnings`。本轮实时扫描结果为 core 6 个、desktop 23 个 allowed warning,均非未忽略 vulnerability。

---

## 修复进度(第二轮 · Medium/Low 批量,2026-07-26)

在第一轮(全部 Critical + High)之后,按模块继续处理 Medium/Low。摘要:

**core 加密 / vault / store**
- CORE-5 ✅ Argon2 参数下限(`MIN_ARGON2_*` = 19 MiB/t2/l1,OWASP 阈值),`derive_key_argon2id` 双向校验;测试参数同步上调。
- CORE-6 ✅ 版本号分配改 `Store::next_version()`——`vault_meta` 内单事务原子自增计数器(种子取 `MAX(version)`,无 schema 迁移),消除 `max_version()+upsert` 之间的 TOCTOU。
- CORE-2 ✅ 删除误导性"防回滚"注释,改为准确的威胁模型说明(AAD 绑定版本号但不防本地库整行回旧;全局高水位对全局唯一版本无效)。
- CORE-3 ✅ `decrypt_record` 及 `Vault::get/list`、`DirtyRecord/FullRecord.plaintext` 改 `Zeroizing<Vec<u8>>`,解密明文用后即擦。
- CORE-8 ✅ store/ffi/app/term 的 `Mutex::lock().unwrap()`(91 处)改 `unwrap_or_else(PoisonError::into_inner)`,锁毒化不再连锁 panic。
- CORE-9 / SSH-14 ✅ `DirtyRecord`/`FullRecord`/`ConflictRow`/`AuthMethod`/`HostAuth` 手写脱敏 `Debug`。
- CORE-4 / CORE-7 ⏸ 延后:改 AAD/verifier 域分离会使既有 vault 无法解密(需整库重加密),CORE-7 且审计判定非漏洞——为 Low/非漏洞破坏兼容性不划算,记为后续项。

**sync**
- SYNC-10 ✅ `read_manifest_required` 加重试退避,缓解 SFTP `write_atomic` 先删后改名的瞬时 None。
- SYNC-11 ✅ compact 后从 `applied_events` 剔除已 trash 事件的 id(因果守卫已使重放安全),集合不再无界增长。
- SYNC-12 ✅ local/SFTP 适配器 `read` 先 stat 校验大小,超 64 MiB 拒绝,防恶意后端 OOM。
- SYNC-13 ✅ join 优先试本机 `device_id` 条目,其余至多试 64 条,限制不可信 keyring 的 Argon2 DoS。
- SYNC-14 ✅ local/SFTP 写路径 key 统一 `validate_repo_relative_path`(防 `repo::join` 的 `..` 逃逸)。
- SYNC-15 ✅ 随第一轮 apply_snapshot 重写已消解(单次 `find` + `?`)。
- SYNC-8 ⏸ 延后:撤销绕过的根因是根密钥轮换未实现(M7),签 keyring 亦无济于事(被撤设备已知根密钥);manifest MAC 需向后兼容/防降级设计。属已文档化 M7 限制,留作专门一轮。

**ssh / ffi**
- SSH-6 ✅ `set_network_proxy` 移除运行时 `env::set_var`,代理仅存进程内 `RwLock`;`ai_http_client` 显式 `.no_proxy().proxy(...)` 读取,消除 setenv/getenv 数据竞争。
- SSH-8 ✅ AI `base_url` 非回环主机强制 https(`validate_ai_base_url`),防 Bearer key 明文外发。
- SSH-10 ✅ SFTP 覆盖下载改临时文件 + 成功后 rename,失败只删临时文件,不再销毁用户原文件。
- SSH-11 ✅ SOCKS5 `handle_socks` 的 `copy_bidirectional` 纳入 cancel token 的 `select!`。
- SSH-12 ✅ host-key 提示 `ask()` 加 RAII `PendingGuard`,连接超时取消 future 时也清理 pending,不再泄漏 oneshot sender。
- SSH-4 / SSH-5 / SSH-7 ⏸ 延后:SSH-4/5 需把 `Arc<Handle>` 改 `Arc<Mutex<Handle>>`(russh 0.45 `tcpip_forward` 为 `&mut self`,Handle 不可 Clone),会串行化所有 channel 操作;SSH-7 需把跳板会话生命周期穿过三个 FFI 入口。均为 Medium/Low 可用性问题(非安全),且无法在无真实 SSH 服务器时验证,留作专门一轮(已在 `session.rs` 注释记录)。

**android**(子 agent,`:app:compileDebugKotlin` 通过)
- AND-2 ✅ `MainActivity` 设 `FLAG_SECURE`(全局)。AND-3 ✅ `SessionManager` 各移除点确定性 `terminal.destroy()`。AND-4 ✅ release 缺签名环境变量时构建失败(不再回退 debug key)。AND-5 ✅ APK 更新校验 GitHub 资产 `sha256` 摘要(有则强制,无则告警不静默安装)。AND-6 ✅ `AutoSyncController` 日志裹 `BuildConfig.DEBUG`。AND-7 ✅ `MainActivity.handleIntent` 校验内部 marker + referrer。AND-8 ✅ `UnlockViewModel` 状态查询移 `Dispatchers.IO`。

**frontend**(子 agent,`node --check` + 脱敏回归通过)
- FE-3 ✅ `closeTab`/`disconnectPaneSession`/`closeActiveSplit` 清理四个 per-pane Map(新增 `forgetAiPaneState`)。FE-4 ✅ AI 建议命令执行前 `openConfirmDialog` 预览确认(单击与代码块两路)。FE-6 ✅ Markdown href 协议白名单(仅 http/https/mailto)。FE-8 ✅ 全局 `unhandledrejection` 兜底。

**tauri 桌面后端**(子 agent,`cargo build` 无告警,`cargo test --lib` 42/42)
- TAURI-2 ✅ 删除运行时 `env::set_var`,代理入进程内 `RwLock`,统一 `build_http_client` 走 `.no_proxy().proxy(...)`。TAURI-3 ✅ `network-proxy.json` 只存 scheme/host/port,userinfo 入 keychain(`__network_proxy__`),用时重组 + 来源校验 + 启动迁移旧明文。TAURI-4 ✅ host-key 提示 `tokio::time::timeout(120s)` 超时按 Reject + RAII guard 清理 pending。TAURI-5 ✅ `rewind_bytes` 改 `fetch_update` CAS 饱和到 0。TAURI-7 ⏸ 无 vault 通用 blob 加密 API,退为 `0600` 权限加固 + 明文说明(未发明加密)。TAURI-8 ✅ `sftp_upload`/`set_background_image` 阻塞 IO 改 `tokio::fs`。TAURI-9 ✅ 下载暂存改 `create_new`(O_EXCL)拒符号链接。

**供应链 / CI**
- DEP-4 ✅ 删除陈旧 `core/downloaded.toml`。DEP-3 ✅ 新增 `.github/workflows/ci.yml`(PR/push 触发:core `cargo test`+`clippy -D warnings`、`cargo audit`、前端 `node --check`+脱敏测试、desktop 后端 test);顺带清零 core 首方 clippy 告警。DEP-2 ✅ `.cargo/audit.toml` 记录并 ignore rsa RUSTSEC-2023-0071(无补丁,威胁说明)。DEP-1 ⏸ russh 0.45→≥0.54.1 为破坏性 API 升级,无真实 SSH 服务器无法验证,延后并在 audit.toml + CI(audit job 暂设 `continue-on-error`)显式追踪。

> 说明:延后项(CORE-4/7、SYNC-8、SSH-4/5/7、DEP-1)均为破坏向后兼容、需专门验证环境、或依赖未实现的 M7 前置(根密钥轮换)的改动,已在代码/配置注释就地记录,不是遗漏。core 全部单测通过(21 套),core clippy `-D warnings` 零告警;`alacritty_terminal` 为 vendored 第三方,已在 CI 排除。

---

## 修复进度(2026-07-26 更新)

已按严重程度处理最高优先级的 10 项(全部 Critical + 全部 High)以及若干横切项:

| 编号 | 状态 | 落地方式 |
|---|---|---|
| **SYNC-1/3/4/7** | ✅ 已修 | 记录新增 `(last_clock, last_device)` 因果元数据(store schema v3);`apply_event` 重写为按 `(lamport_clock, device_id)` 全序的可交换合并——幂等/陈旧丢弃/本地脏冲突/快进/并发分叉五档;墓碑进快照且 apply 按 clock 判定防复活;并发双方对称登记冲突;apply/compact 按 clock(而非墙钟月份桶)排序。 |
| **SYNC-2** | ✅ 已修 | `mark_clean` 改 compare-and-clear:仅当行 `local_rev` 仍等于 push 起始值才清脏;push 窗口内的再编辑保持 dirty,下轮作为 fast-forward 推送。 |
| **SYNC-5** | ✅ 已修 | apply/compact 对单事件解码/解密失败改 `warn`+`continue`,超过宽限期移入 `trash/corrupt-<ts>/`;local/SFTP `write_new` 改临时文件 + rename 崩溃原子。 |
| **SYNC-6** | ✅ 已修 | `bump_manifest_head_clock` 与 compact 写 manifest 前重读并 `max` 合并 `head_clock`、保留更新的 `latest_snapshot`;compact 顺序改为写快照→写 manifest→再 trash 事件;`sync_once`/`compact`/`join`/`create` 统一走 engine 级 `op_lock` 串行化(顺带修 SYNC-9)。 |
| **CORE-1** | ✅ 已修 | 冲突 payload 用主密钥 AEAD 加密后落库(新增 nonce 列);解锁时惰性重加密历史明文行;`secure_delete=ON` 清残留页;`ConflictRow`/`DirtyRecord`/`FullRecord` 手写脱敏 `Debug`(顺带 CORE-9)。 |
| **SSH-1/SSH-13** | ✅ 已修 | `rsa-sha2-256/512` 归一化为 `ssh-rsa`,仅以 key blob 判定 Trusted/Mismatch;主机名 ASCII 大小写不敏感;顺带实现哈希主机名(HMAC-SHA1)匹配与 `@revoked` 硬拒。SSH-2 的完整 `@cert-authority` 支持见第三轮权威状态。 |
| **TAURI-1** | ✅ 已修 | 新增 `pick_local_file` 授权表:`read_local_text_file` 仅接受本会话经原生对话框选中的路径且限 1 MiB;`open_with_app` 的应用路径限白名单(选中项或系统应用目录)并 `--` 隔离参数;`docker_exec` 子命令白名单(禁 run/exec/cp/build);`local_remove_dir` 禁根/顶层/home 及其祖先。 |
| **FE-1/FE-2/FE-7** | ✅ 已修 | 脱敏支持引号包裹含空格值(匹配到闭合引号);键名前后缀与 URL scheme 限长消除 O(n²) 回溯 + 输入长度上限;脱敏前剥离 ANSI 转义。新增 `desktop/tests/redact.test.js` 回归。 |
| **AND-1** | ✅ 已修 | `MasterPasswordStore` 改用 Keystore AES-GCM 密钥,`setUserAuthenticationRequired(true)` + `AUTH_BIOMETRIC_STRONG` + `setInvalidatedByBiometricEnrollment(true)`;存/取均经 `BiometricPrompt` 的 `CryptoObject` 事务;等级提升到 `BIOMETRIC_STRONG`。 |

其余 Medium/Low 项(zeroize、锁毒化、AI 会话加密、FLAG_SECURE、依赖升级、CI 门禁等)未在本轮处理,建议按报告「优先修复路线图」的 P1/P2 继续排期。核心 crate 与桌面后端单测、新增回归全部通过;`alacritty_terminal` 的 `--test ref` 编译失败为 vendored crate 既有问题(与本轮改动无关)。

---

## 执行摘要

| 严重级别 | 数量 | 代表问题 |
|---|---|---|
| **Critical** | 2 | 同步 apply 无因果守卫致静默丢更新;push 窗口内本地编辑被静默清脏永不上云 |
| **High** | 9 | 冲突记录明文落盘(绕过 E2E 静态加密)、删除记录复活、并发冲突不对称丢失、坏对象瘫痪整仓同步、manifest 并发丢更新、RSA host key 校验与 OpenSSH 不兼容、IPC 命令面过宽=RCE、脱敏绕过外发凭据、Android 生物识别未加密绑定 |
| **Medium** | 24 | 回滚防护失效、明文不 zeroize、`env::set_var` 数据竞争、代理凭据明文落盘、脱敏 ReDoS、前端内存泄漏、AI 命令一键执行、FLAG_SECURE 缺失、release 回退 debug 签名、依赖公告(russh/rsa)、CI 无测试/审计门禁 等 |
| **Low** | 33 | 锁毒化、`Debug` 打印明文、AI 会话明文落盘、覆盖下载失败删原文件、各类资源泄漏与健壮性细节 |

> 合计 68 项。以下按模块详列,每条含类别、`文件:行号`、触发条件与修复建议。

### 最需优先处理的 10 项

1. **[Critical] SYNC-1** 同步合并只看本地 `dirty`,从不比较 Lamport 时钟 → 乱序旧事件静默覆盖新值 + 永久分歧
2. **[Critical] SYNC-2** push 上传窗口内的再编辑被 `mark_clean` 无条件清脏 → 单设备即可稳定复现的永久丢失
3. **[High] CORE-1** 同步冲突记录以**明文**写入本地 SQLite,含密码/passphrase/私钥 PEM → 绕过 Argon2id+AEAD 静态加密
4. **[High] SSH-1** RSA host key 的 known_hosts 比对与 OpenSSH 不兼容 → 合法主机被判 Mismatch,训练用户接受 MITM 告警
5. **[High] TAURI-1** IPC 命令面过宽(`open_with_app` 可启动任意可执行文件 = RCE),配合 `withGlobalTauri` 使任何 XSS 升级为任意命令执行
6. **[High] FE-1** 前端脱敏对含空格的引号值只脱敏首词 → 凭据部分明文外发第三方 AI,而 UI 承诺"已脱敏"
7. **[High] AND-1** Android 生物识别仅为 UI 门禁,未与 Keystore 密钥加密绑定 → 可绕过指纹取主密码
8. **[High] SYNC-5** 单个损坏事件对象使所有设备每轮 apply/compact 中止 → 后端一个坏对象永久瘫痪整仓同步
9. **[High] SYNC-6** manifest 无条件写(非 CAS)→ 并发 push / compaction 竞态使 `latest_snapshot` 指针回退、事件丢失
10. **[High] SYNC-3 / SYNC-4** 删除记录复活;并发编辑冲突检测不对称,"先推方"被静默覆盖且无冲突记录

---

## 横切主题(多个模块交叉印证)

这些问题在多个 crate/层重复出现,建议作为统一整改项:

- **敏感明文全程不 zeroize**:vault 对主密钥严格 `Zeroizing`,但解密后的凭据明文 `Vec<u8>`、口令 `String`、SSH `AuthMethod` 从不清零。见 CORE-3、SSH-3。
- **`Debug` 派生打印明文机密**:`AuthMethod`、`HostAuth`、`DirtyRecord`、`FullRecord` 派生 `#[derive(Debug)]` 含明文,当前无调用点触发,属"新增一行日志即泄密"的脚枪。见 CORE-9、SSH-14。
- **运行时 `std::env::set_var` 数据竞争**:代理配置在多线程进程改环境变量,可致段错误。前后端两处。见 TAURI-2、SSH-6。
- **`Mutex::lock().unwrap()` 锁毒化**:遍布 store/ffi/tauri,单次持锁 panic 使相关子系统在进程内永久不可用。见 CORE-8、SSH-9、TAURI-6。
- **AI 会话明文落盘**:对话历史(含用户输入原文/AI 回复)明文存 config 目录,游离于加密 vault 之外。见 TAURI-7、FE-5。
- **依赖 `russh-keys` 引入的 RSA 私钥操作与 host key 校验**:同时受 SSH-1(校验逻辑)与 DEP-2(rsa Marvin 侧信道)影响。

---

## A. 供应链与 CI/CD

### DEP-1 [Medium] russh 0.45.0 命中 CVE-2025-54804(channel window adjust 整数溢出)
- **位置**:`core/Cargo.lock`(russh 0.45.0)、`core/crates/zeroterm-ssh/Cargo.toml`
- **问题**:russh < 0.54.1 的 `SSH_MSG_CHANNEL_WINDOW_ADJUST` 处理存在整数溢出(GHSA-h5rc-j5f5-3gcm,CVSS 6.5)。作为 SSH **客户端**同样处理服务器发来的 window adjust 报文,恶意/被入侵服务器可致客户端 panic(debug 编译)或窗口记账损坏(release)。CVE-2024-43410(0.44.1 修复)在 0.45 已不受影响。
- **修复**:升级 russh 至 ≥ 0.54.1(注意 API 变更);引入 `cargo audit`/`cargo deny` 到 CI。

### DEP-2 [Medium] rsa 0.9.10 命中 RUSTSEC-2023-0071(Marvin 时序侧信道,无官方补丁)
- **位置**:`core/Cargo.lock`(rsa 0.9.10,经 `russh-keys` 引入)
- **问题**:rsa crate 非常量时间实现,可通过网络可观测的时序恢复 RSA 私钥(CVE-2023-49092)。截至目前**无修复版本**。在 SSH 客户端场景实际可利用性有限(pubkey 签名非攻击者选择输入的解密预言机),但属长期在案公告。
- **修复**:优先使用 Ed25519/ECDSA 密钥而非 RSA;关注 rsa crate 常量时间实现进展;`cargo audit` 纳入监控并显式 `ignore` + 备注威胁评估。

### DEP-3 [Medium] CI 无任何测试 / 审计 / lint 门禁
- **位置**:`.github/workflows/release.yml`(仓库唯一工作流)
- **问题**:CI 只有发布流程,**无 `cargo test` / `cargo clippy` / `cargo audit` / `cargo deny` / 前端测试**。核心 crate 有可观的单测(sync/store/crypto 均有),但从不在 CI 运行,回归与新引入漏洞无自动拦截。
- **修复**:新增 PR 触发的 CI:`cargo test --workspace`、`cargo clippy -- -D warnings`、`cargo audit`;桌面/前端加基本冒烟。

### DEP-4 [Low] `core/downloaded.toml` 陈旧的重复 workspace 清单
- **位置**:`core/downloaded.toml`
- **问题**:一份过时的 workspace 清单副本(`version = "0.0.1"`,缺 `zeroterm-sync`/`zeroterm-term` 成员,`repository` 指向 `NotPeppa/ZeroTerm` 而非当前仓库)。易造成混淆,repo URL 差异也可能是 fork/改名残留。
- **修复**:删除该文件;若为某脚本产物,加入 `.gitignore`。

---

## B. 核心加密 / Vault / Store

### CORE-1 [High] 同步冲突记录以明文写入本地 SQLite,绕过静态加密
- **类别**:crypto-at-rest / data-confidentiality
- **位置**:`zeroterm-store/src/lib.rs:120-134`(`sync_conflicts` 表 `local_payload`/`remote_payload` 明文 BLOB)、`:379-401`;`zeroterm-vault/src/vault.rs:407-429`;`zeroterm-sync/src/engine.rs:502-524`;`zeroterm-app/src/sync.rs:958-978`
- **问题**:金库承诺凭据在盘上恒为 XChaCha20-Poly1305 密文,但冲突检测路径把**解密后的明文**原样写入 `sync_conflicts` 表。对 `kind=="host"` 记录,明文含 `auth.value`(密码)、`auth.passphrase`、`auth.key_pem`(私钥)。任意记录发生一次同步冲突即触发,`trim_resolved_conflicts` 还保留最近 10 条已解决冲突,明文长期驻留。
- **后果**:拿到本地 SQLite(丢失设备/云备份/无口令的恶意软件)即可读出曾冲突过的主机密码与私钥,实质绕过 E2E 静态加密。
- **修复**:冲突 payload 落库前用主密钥(或专用子密钥)加密(复用 `encrypt_record` 语义,加 `nonce` 列);或只存 `record_id + 两个 revision` 引用,解决时按需解密。

### CORE-2 [Medium] 版本回滚防护实际无效,与代码注释声称不符
- **类别**:crypto-integrity / anti-rollback
- **位置**:`zeroterm-vault/src/crypto.rs:123-158`;`zeroterm-vault/src/vault.rs:207-222`
- **问题**:`decrypt_record` 计算 AAD 的 `version` 取自**同一条被读取的记录行**(`rec.version`),该 version 与密文/nonce 同存一行,无受信锚点。攻击者(可写本地库、无口令)将某行整体替换为该记录**自身历史旧三元组** `(ciphertext_old, nonce_old, version_old)`,解密照样成功 → 记录被静默回滚到旧明文。crypto.rs 注释声称"不能回滚到旧版本"未兑现。
- **修复**:引入受信单调锚点——在加密的 verifier 或独立 MAC 中存全局 `max_version` 高水位并校验;或明确将"本地文件回滚"移出威胁模型并删除误导性注释。

### CORE-3 [Medium] 解密后的明文机密从不 zeroize(横切)
- **类别**:credential-residue
- **位置**:`zeroterm-crypto/src/lib.rs:173-192`(`aead_decrypt` 返回裸 `Vec<u8>`);`zeroterm-vault/src/vault.rs:207-388`(`get`/`list`/`list_dirty`/`find_full` 返回明文);口令 `zeroterm-vault/src/crypto.rs:104-110`、`zeroterm-ffi/src/sync_api.rs:67`
- **问题**:主密钥用 `Zeroizing`,但下游最敏感的解密明文与口令以普通 `Vec<u8>`/`String` 流转,用后不清零,残留在释放的堆内存,放大内存转储/交换/冷启动取证暴露面。
- **修复**:`decrypt_record` 返回 `Zeroizing<Vec<u8>>`;`DirtyRecord.plaintext`/`FullRecord.plaintext`、口令改 `Zeroizing`/`secrecy`;减少不必要 clone。

### CORE-4 [Low] 行元数据(kind/deleted)未纳入 AEAD 认证
- **类别**:metadata-tamper
- **位置**:`zeroterm-vault/src/crypto.rs:123-128`;`zeroterm-store/src/lib.rs:37-59`;下游 `zeroterm-app/src/sync.rs:844-853`
- **问题**:AAD 仅含 `record_id + version`,`kind`/`deleted`/`updated_at` 明文且不受认证。可写本地库者翻转 `deleted=1` 隐藏存活记录;篡改 `kind` 不影响解密。**已核实的下游后果**:`conflict_preview`(sync.rs:844-853)只对 `kind=="host"` 掩码密码,把 host 记录的 `kind` 改为 `host_group`/`snippet`/`port_forward` 会使冲突预览**返回含密码的原始 JSON**。因冲突 payload 本已明文落盘(CORE-1),此项影响被前者部分涵盖。
- **修复**:AAD 纳入 `kind` 与 `deleted`,如 `record_id || version || kind || deleted`。

### CORE-5 [Low] Argon2 参数只校验上限、无下限
- **位置**:`zeroterm-crypto/src/lib.rs:27-29, 108-118`;`zeroterm-vault/src/vault.rs:42-46`;`zeroterm-sync/src/keyring.rs:133-140`
- **问题**:`derive_key_argon2id` 拒绝超上限但不拒绝异常弱参数(如 8 KiB/1/1)。非降级攻击(改参数会导致解密失败),真实风险是调用方误配产生永久弱化的 vault。
- **修复**:加 `MIN_ARGON2_*` 下限(如 ≥ 19 MiB、t≥2),低于则 `InvalidParams`。

### CORE-6 [Low] 版本号分配 TOCTOU 竞态 → 重复 version → 同步增量跳记录
- **位置**:`zeroterm-vault/src/vault.rs:120-282`;`zeroterm-store/src/lib.rs:231-278`
- **问题**:`insert`/`update`/`delete` 先 `max_version()`(一次加锁读)再 `upsert_record`(另一次加锁写),两次之间无事务。并发 Tauri 命令可致两写读到同一 `max_version` 都写成 `N+1`,version 重复;`records_since` 用 `version > since`,重复/非递增 version 可能使记录在增量拉取中被跳过。
- **修复**:将"读 max_version + 写"放入单事务原子自增(`INSERT ... SELECT COALESCE(MAX(version),0)+1`)。

### CORE-7 [Low] verifier 直接以主密钥作 AEAD 密钥,缺域分离
- **位置**:`zeroterm-vault/src/crypto.rs:162-166`
- **问题**:`encrypt_verifier` 把主密钥直接当 XChaCha 密钥,而主密钥又作记录密钥的 HKDF IKM。不构成 nonce/密钥复用漏洞,但同一密钥材料两用不符合域分离最佳实践。
- **修复**:verifier 也经 HKDF 派生独立子密钥(distinct `info`)。

### CORE-8 [Low] Store `Mutex::lock().unwrap()` 锁毒化连锁 panic(横切)
- **位置**:`zeroterm-store/src/lib.rs`(167/176/190/218/232/241 等所有方法)
- **问题**:任一持锁期间 panic 使锁毒化,之后所有 vault 操作 `.unwrap()` 连锁 panic,进程内不可用。
- **修复**:改 `parking_lot::Mutex`(不毒化)或 `lock().unwrap_or_else(|e| e.into_inner())`。

### CORE-9 [Low] 携带明文的结构体派生 `Debug`,潜在日志泄漏(横切)
- **位置**:`zeroterm-vault/src/vault.rs:483-505`(`DirtyRecord`/`FullRecord`);另见 SSH-14
- **问题**:派生 `Debug` 含 `plaintext: Vec<u8>`,当前无 `{:?}` 日志触发,但将来一行 `tracing::debug!(?record)` 即泄露口令明文。
- **修复**:手写脱敏 `Debug`(明文字段打印为 `<redacted; N bytes>`)。

---

## C. 同步引擎(zeroterm-sync)

> 同步引擎是本次审计问题最集中、最高危的子系统。C1/H3/H4/H2(apply 路径)共享同一根因:`apply_event` 仅凭本地 `dirty` 布尔决策合并,**从不比较 `lamport_clock`/`parent_revision`**(事件写入了 `parent_revision` 却在 engine 内零处读取)。

### SYNC-1 [Critical] apply 无因果守卫,乱序旧事件静默覆盖新值
- **类别**:data-loss / stale-overwrite(multi-device)
- **位置**:`zeroterm-sync/src/engine.rs:467-527`(干净副本无条件采纳:upsert `490-495`、delete `496-499`);应用循环 `:405-465`;生产存储无版本比较 `zeroterm-vault/src/vault.rs:257-311`
- **场景**:A 写 R=V1(clock10)推送;C 拉后写 R=V2(clock11)推送;B 经云文件夹/S3 最终一致**先收到 E_C**(→V2,clean)、**后收到 E_A**(pass2)→ `(Some clean, Upsert)` 采纳 V1 → R 静默回退,V2 全网丢失并被 compaction 固化进快照。延迟的旧 `Delete` 命中已更新记录则直接打墓碑使记录消失。LocalFolder/S3 等乱序投递后端为常态触发。
- **修复**:记录持久化 `last_clock`,apply 前 `if ev.lamport_clock <= local.last_clock { skip }` 的 LWW 守卫;delete 同按 clock 判定。见文末「统一合并规范」。

### SYNC-2 [Critical] push 窗口内的并发本地编辑被 `mark_clean` 无条件清脏 → 永久丢失
- **类别**:lost-update / data-loss(single-device)
- **位置**:`zeroterm-sync/src/engine.rs:547-602`(`mark_clean` 调用于 `599`);无条件清脏 `zeroterm-store/src/lib.rs:363-375`(`UPDATE records SET dirty=0 ... WHERE id=?1`)
- **场景**:单设备单用户即可稳定复现。`list_dirty()` 克隆脏记录 V1(`local_rev=rA`)→ `write_new` 异步上传(慢后端秒级)→ 用户在窗口内再编辑同记录 V2(`local_rev=rB`,dirty=1)→ 上传完成 `mark_clean` **无条件**清脏 → V2 被标 clean 却从未推送,永不外发;之后任一远端改动会覆盖 V2。
- **修复**:`mark_clean` 改条件清脏——仅当行当前 `local_rev` 仍等于 push 起始读到的 `rec.local_rev` 才清 dirty(compare-and-clear),扩展 store API 传入期望前置 `local_rev`。

### SYNC-3 [High] 删除记录复活 / 僵尸墓碑
- **类别**:resurrection / data-integrity
- **位置**:压缩 safety-net `zeroterm-sync/src/engine.rs:862-902`;apply 墓碑上采纳 upsert `:490-495`;`zeroterm-vault/src/vault.rs:344-363`(`find_full` 对墓碑返回 `deleted=true`)、`:257-282`(`apply_remote_upsert` 无条件复活)
- **场景**:B 在 A 删除 R 期间离线;A 压缩把 delete 事件清入 trash、快照已不含 R → B 上线后快照无 R、events 无 delete → B 永远学不到删除,本地 R 存活 → B 一次 compact 经 safety-net(只看 `working.contains_key` 与自身 `rec.deleted`,**从不查墓碑**)把 R 重新 seal 进快照 → 全网复活。
- **修复**:safety-net 纳入前须确认本地无该 id 墓碑且仓库无对应 delete;墓碑保留期严格长于事件保留期;apply 墓碑上 upsert 按 clock 判定。

### SYNC-4 [High] 并发编辑冲突检测不对称,"负方"设备静默丢失且无提示
- **类别**:data-loss / conflict-asymmetry
- **位置**:`zeroterm-sync/src/engine.rs:489-495`(干净侧采纳、**不记冲突**)对比 `:502-513`(脏侧记冲突);测试仅验证胜方 `zeroterm-sync/tests/local_roundtrip.rs:102-129`
- **场景**:A、B 并发改 R。A 先推→变 clean;B 后推时本地 dirty→记冲突并保留自值(B 值全网胜出);A 再同步时 R 已 clean→**静默采纳 B 值、不产生任何冲突记录**。被丢弃的一方(A)零信号;拿到冲突条目的反而是值被保留的 B。B 忽略收件箱则 A 的编辑永久消失。
- **修复**:采纳干净侧远端 upsert 前用 `parent_revision`/clock 判定并发分叉;并发时**双方对称登记冲突** + `(clock, device_id)` 确定性裁决保证收敛。

### SYNC-5 [High] 单个损坏/半写事件对象使全体设备 apply 与 compact 整轮中止
- **类别**:poison-object / availability
- **位置**:apply 解码即传播 `zeroterm-sync/src/engine.rs:425`;compact 同 `:834`;非崩溃原子写——本地 `adapter/local.rs:147-168`(`create_new` 后 `write_all`,中断留半文件于最终路径)、SFTP `adapter/sftp.rs:309-329`(直接 `upload_from_slice` 到最终路径)
- **场景**:一次被中断的 `write_new`(崩溃/移动网络掉线)在 `events/` 留下空/截断文件 → `decode_event` 返回 `Corrupt`,`?` 中止整轮 → 之后**每台设备每次同步都在该文件处中止**,脏编辑堆积无法外发,compact 也无法清理。恶意后端丢一个不可解码对象即永久瘫痪整仓同步,无自愈。
- **修复**:apply/compact 对单事件解码失败改为 `warn` + `continue`(移入 `trash/corrupt/`);本地/SFTP 的 `write_new` 采用临时文件 + rename 实现崩溃原子。

### SYNC-6 [High] manifest 无条件写(非 CAS),并发 push / compaction 使 `latest_snapshot` 指针回退
- **类别**:manifest-lost-update / compaction-race
- **位置**:`bump_manifest_head_clock` 拷贝陈旧 `latest_snapshot` `zeroterm-sync/src/engine.rs:734-757`(拷贝在 `748`),sync_once 调用于 `:395`、pass 起始读 manifest 于 `:358`;compact 先 trash 事件 `:925-939` 后写 manifest `:941-953`;各适配器 `put_manifest` 无条件覆盖 `adapter/webdav.rs:243-271`、`adapter/sftp.rs`、`adapter/local.rs`
- **场景**:X 的 sync_once 在 pass 起始读到旧 manifest;同时 Y 的 compact 把 `latest_snapshot` 指向新快照并把被折叠事件清入 trash。X 推送后 `bump` 重写 manifest,`latest_snapshot` 原样拷贝旧值 → **指针回退**。新快照在盘却不被引用、其覆盖的事件已进 trash → 新 joiner 读旧快照 + 现存 events(缺 trash 的)→ 丢失只存在于被折叠事件里的记录。近乎同时的两设备 push 同理互相覆盖 manifest。
- **修复**:manifest 条件写(ETag/If-Match、S3 版本、WebDAV LOCK、本地 `O_EXCL`+rename+重读);bump 前重读 manifest,`max` 合并 `head_clock` 并保留更新的 `latest_snapshot`;compact 顺序改为"写快照→写 manifest→再 trash 事件"。

### SYNC-7 [Medium] 事件应用/压缩顺序由墙钟月份桶主导而非 Lamport 时钟
- **类别**:ordering(SYNC-1 同根症状)
- **位置**:apply 排序 `zeroterm-sync/src/engine.rs:413-419`(`event_paths.sort()` 按完整路径);compact 排序 `:817-822`;月份桶来自墙钟 `repo.rs:78-110`
- **场景**:排序最外层是 `events/YYYY-MM/` 墙钟桶而非 clock。NTP 回拨跨月边界:04-01 写 edit1(桶 2024-04,clock10)→回拨 03-28 写 edit2(桶 2024-03,clock11)→ 干净副本上 edit1 排在 edit2 后应用 → 较新的 edit2 被覆盖。
- **修复**:apply 与 compact 显式按 `lamport_clock`(平局 `device_id`)排序;根治随 SYNC-1 消解。

### SYNC-8 [Medium] manifest/keyring 无认证防回滚;keyring 撤销可"删后重塞"绕过
- **类别**:metadata-tamper / revocation-bypass
- **位置**:manifest 无 MAC `zeroterm-sync/src/manifest.rs`(`from_json` 仅查 schema `52-65`);keyring 条目集合无整体认证 `keyring.rs:47-52, 90-94`;根密钥轮换未实现 `keyring.rs:24-26`
- **场景**:记录**内容**有 per-record AEAD(稳健),但**元数据**不受根密钥保护。被入侵后端可改 manifest 把 `latest_snapshot` 指向旧快照 + 删近期事件 → 无察觉回滚到陈旧态;被撤销旧设备只要仍有写权限即可把自己**重新加回** keyring(撤销仅靠删条目、轮换未实现),使"移除设备"形同虚设。属主动攻击 + 已文档化 M7 限制,故 Medium。
- **修复**:manifest 增根密钥派生 MAC + 单调版本号;keyring 对条目集合整体签名;落地根密钥轮换。

### SYNC-9 [Medium] 同一 engine 上 sync_once / compact 无并发串行化
- **类别**:non-idempotent / race
- **位置**:`zeroterm-app/src/sync.rs:656-667`(`sync_now`)、`:721-734`(`sync_compact`)均无 in-flight 锁;去抖仅 abort `sync.rs:276-306`;engine 整轮无锁(仅 `EngineState` 有 per-op tokio mutex)
- **场景**:手动/去抖/恢复全量/手动 compact 可在同一 `Arc<SyncEngine>` 重叠。两个 sync_once 并发→重复推送同一脏记录→重复事件;`applied_events` 全量写相互覆盖→丢条目→事件重复 apply(再触发 SYNC-1/3)。sync_once + compact 叠加即 SYNC-6。
- **修复**:每 profile/engine 一把"同步进行中"互斥,串行化整轮 sync 与 compact。

### SYNC-10 [Medium] SFTP `write_atomic` 为"先删后改名",有可见空窗且非跨写者原子
- **类别**:atomicity
- **位置**:`zeroterm-sync/src/adapter/sftp.rs:331-364`(`upload tmp` → `remove_file(path)` → `rename(tmp,path)`)
- **场景**:manifest/keyring 走此路径。`remove` 与 `rename` 间存在文件不存在窗口 → 并发读者返回 None → 上层瞬时报 `NotInitialized`。
- **修复**:优先用服务端 posix-rename 扩展原子替换;或读侧对 manifest/keyring 的瞬时 None 重试。

### SYNC-11 [Medium] `applied_events` 无界增长且每轮全量重写
- **类别**:scalability
- **位置**:压缩刻意不清理 `zeroterm-sync/src/engine.rs:966-975`;load/persist 全量 JSON `:1136-1151`
- **修复**:以"已应用到的最大连续 clock 低水位 + 近期例外集合"取代 id 全集,随快照修剪。

### SYNC-12 [Low] 各适配器对象读取无大小上限 → 恶意后端可 OOM
- **位置**:`adapter/local.rs:76-82`、`sftp.rs:262-269`、`webdav.rs:226-238`、`s3.rs:287-311`
- **修复**:对 manifest/event/snapshot 读取设合理大小上限。

### SYNC-13 [Low] join 对每个不可信 keyring 条目跑满配额 Argon2id,条目数无限制(DoS)
- **位置**:`zeroterm-sync/src/engine.rs:281-286`;单条参数上限 `zeroterm-crypto/src/lib.rs:113-118`
- **场景**:后端塞入成百上千条 256MiB/10-iter 条目,受害者 join 被迫串行跑完全部昂贵 KDF。`sync_once` 缓存 root key 不受影响,故限 join。
- **修复**:限制条目数(如 ≤64)与单次 join 总 KDF 预算;优先只试匹配本机 `device_id` 的条目。

### SYNC-14 [Low] 写路径 key 未做遍历校验(仅列举返回的路径校验)
- **位置**:`validate_repo_relative_path` 仅用于后端列举 `adapter/mod.rs:27-38`;写路径 key 未校验;`repo::join` 会 `push("..")` `repo.rs:131-139`
- **说明**:当前不可利用(`device_id` 已 sanitize、其余分量为数字/UUID),缺纵深防御。
- **修复**:对所有写路径 key 统一 `validate_repo_relative_path`。

### SYNC-15 [Low] `apply_snapshot_if_new` 两次 `find()` + `.unwrap()`,并发删除下 panic
- **位置**:`zeroterm-sync/src/engine.rs:702-709`
- **修复**:复用第一次 `find` 结果,`if let Some(loc) = ...`。

---

## D. SSH / SFTP / FFI

### SSH-1 [High] RSA host key 的 known_hosts 比对与 OpenSSH 不兼容 → 破坏校验并诱导接受"密钥变更"
- **类别**:host-key-verification-break / warning-fatigue
- **位置**:`zeroterm-ssh/src/known_hosts.rs:81, 105, 136`
- **问题**:`check()` 用 `target_name = key.name()` 与文件 `keytype` 逐字比较(`105`:`keytype == target_name && b64 == target_b64`)。现代 OpenSSH 对 RSA 主机协商 `rsa-sha2-512`/`rsa-sha2-256`,`key.name()` 返回 `"rsa-sha2-512"`,而 known_hosts 文件里合法条目 keytype 恒为 `ssh-rsa`(blob b64 一致)。于是 `"ssh-rsa" == "rsa-sha2-512"` 为假 → **即便密钥字节完全一致也不判 Trusted**,落入 `Mismatch`。
  - **Strict**(SFTP 同步用):对合法 RSA 主机直接拒连,同步不可用。
  - **Interactive**(移动端/CLI 默认):每次连合法未变更 RSA 主机都触发 `on_mismatch`("HOST KEY CHANGED / 疑似 MITM")→ 训练用户习惯性接受 → 瓦解 MITM 防护。
  - `add()` 还写入非标准行 `host rsa-sha2-512 AAAA...`,OpenSSH 不识别,且换算法(rsa-sha2-256)时与自己先前条目自我 mismatch。
- **修复**:比对/写入时把 `rsa-sha2-256`/`rsa-sha2-512` 归一到 `ssh-rsa`;或仅以 `public_key_base64()`(blob)/`fingerprint()` 判定 Trusted/Mismatch,keytype 仅作展示。

### SSH-2 [Medium] known_hosts 忽略哈希主机名 / @revoked / @cert-authority
- **类别**:host-key-bypass(告警降级)
- **位置**:`zeroterm-ssh/src/known_hosts.rs:9-14`(自述不支持)、`:85-121`
- **问题**:不支持 `|1|salt|hash` 哈希主机名(Debian/Ubuntu 默认 `HashKnownHosts yes`)。真实密钥仅以哈希形式存在时,`check()` 无法解析 → 返回 `Unknown`(而非 `Mismatch`)→ Interactive 走温和的"新主机?"而非强烈的"密钥已变更"。攻击者换密钥 MITM 时用户只看到普通新主机提示,接受后 `add()` 还把攻击者密钥落盘。`@revoked` 被当普通行跳过 → **吊销完全失效**;`@cert-authority` 无法识别。
- **修复**:✅ 已完整实现哈希主机名匹配(HMAC-SHA1)、通配/否定模式、`@revoked` 与 `@cert-authority`;vendor russh 最小 KEX 扩展暴露真实主机证书,并严格校验 CA 签名/有效期/host principal/type/critical options。真实 OpenSSH `HostCertificate` Strict E2E 通过。

### SSH-3 [Medium] SSH 层凭据以明文 `String` 常驻且从不 zeroize(横切,见 CORE-3)
- **位置**:`zeroterm-ssh/src/session.rs:25-44`(`AuthMethod`)、`:596/600/607`;`zeroterm-app/src/host.rs:53-65`;`zeroterm-ffi/src/types.rs`
- **问题**:三个 crate `grep zeroize` 无命中。解密后的密码/私钥 PEM/passphrase 流入 `AuthMethod` 后被自由 `clone()`,释放后残留堆内存。
- **修复**:承载密钥/密码/passphrase 的字段改 `Zeroizing<String>`/`ZeroizeOnDrop`;减少 clone。

### SSH-4 [Medium] 远程转发 `-R` 与其它转发/克隆共存时因 `Arc::get_mut` 失败而报错
- **类别**:correctness(可用性)
- **位置**:`zeroterm-ssh/src/session.rs:448`、`forward.rs:192`
- **问题**:`forward_local`/`forward_dynamic` 会 `handle_clone()` 使 `Arc` 计数>1;若 `-L`/`-D` 排在 `-R` 前,`forward_remote` 内 `Arc::get_mut` 返回 `None` → `-R` 静默失效,取决于配置顺序。
- **修复**:去掉 `get_mut` 独占约束,直接在 `Arc<Handle>` 上调 `tcpip_forward`(`&self` 方法)。

### SSH-5 [Medium] 单会话并发多个远程转发互相"窃取并丢弃"入站通道
- **类别**:session-multiplexing-race
- **位置**:`zeroterm-ssh/src/forward.rs:203, 218-263`(尤其 `240-243` 的 `continue`)
- **问题**:所有 `-R` 共享同一 `remote_forward_rx`;两个 `remote_loop` 竞争 `recv()`,发往 B 的通道被 A 取到即因地址不匹配 `continue` 丢弃 → B 永远收不到。
- **修复**:在 handler 侧按 `(bind_addr, bind_port)` 路由,或用多路分发器读单一 receiver 再按目标端口分派。

### SSH-6 [Medium] `set_network_proxy` FFI 调用 `std::env::set_var/remove_var`(横切,见 TAURI-2)
- **位置**:`zeroterm-ffi/src/facade.rs:178-188, 213-222`
- **问题**:对 6 个环境变量 set/remove;多线程(tokio)进程中与其它线程 `getenv`(reqwest/`getaddrinfo`)并发是数据竞争,glibc 上可段错误。
- **修复**:改用已有的 `zeroterm_ssh::set_global_http_proxy`(RwLock);reqwest 构造时显式 `.proxy(...)`,不改全局 env。

### SSH-7 [Low] 移动端交互式会话 / SFTP 忽略 `proxy_jump_host_id`,直连目标
- **位置**:`zeroterm-ffi/src/facade.rs:924-969`、`zeroterm-ffi/src/sftp.rs:101-143`
- **问题**:仅 sync 与 CLI 处理 ProxyJump;FFI 会话/ SFTP 忽略保存的跳板配置直连(host key 仍校验,无 MITM 风险,但绕过用户"必须经跳板"预期,且目标不可直达时失败)。
- **修复**:FFI 会话路径也走 `connect_via`。

### SSH-8 [Low] AI `base_url` 未强制 https,Bearer API Key 可能明文传输
- **位置**:`zeroterm-ffi/src/facade.rs:364/435`(仅 `Url::parse`)、`:400/455/533`(`.bearer_auth`)
- **修复**:对非回环主机要求 https,或至少告警。

### SSH-9 [Low] FFI `Mutex::lock().unwrap()` 锁毒化 → 持续性 DoS(横切,见 CORE-8)
- **位置**:`zeroterm-ffi/src/facade.rs`(`inner`/`sessions`/`sftp_handles` 等)、`session.rs:160/169`
- **修复**:热路径 `lock().unwrap_or_else(|e| e.into_inner())` 或 `parking_lot`。

### SSH-10 [Low] 覆盖式下载失败时删除用户原有本地文件
- **类别**:data-loss
- **位置**:`zeroterm-ffi/src/sftp.rs:218-220, 288`
- **问题**:`overwrite=true` 且目标已存在时,下载中途失败执行 `remove_file(&local)`,把用户原有文件一并删掉。
- **修复**:先下载到临时文件成功后再原子 rename 覆盖。

### SSH-11 [Low] SOCKS5 桥接的 `copy_bidirectional` 未受 cancel 约束
- **位置**:`zeroterm-ssh/src/forward.rs:393`
- **修复**:像 `bridge_to_target` 一样纳入 cancel token 的 `select!`。

### SSH-12 [Low] 主机密钥提示的 oneshot sender 在连接超时/放弃时泄漏(横切,见 TAURI-4)
- **位置**:`zeroterm-ffi/src/listener.rs:87`、`session.rs:356-382`
- **问题**:`ask()` 存入 `pending` 后 `await rx`;15s `connect_timeout` 先触发时 future 被丢弃但 `pending` 条目不清理,每次放弃泄漏一条。
- **修复**:`ask()` 用 RAII guard 在退出时清理自身 request_id。

### SSH-13 [Low] known_hosts 主机名区分大小写(OpenSSH 不区分)
- **位置**:`zeroterm-ssh/src/known_hosts.rs:208`(`entry == host`)
- **修复**:主机名 ASCII 小写归一后比较。

### SSH-14 [Low] `AuthMethod` / `HostAuth` 派生 `Debug` 会明文打印密钥(横切,见 CORE-9)
- **位置**:`zeroterm-ssh/src/session.rs:24`、`zeroterm-app/src/host.rs:53`
- **修复**:手写脱敏 `Debug`(与 `ConnectConfig` 一致)。

---

## E. Tauri 桌面后端

### TAURI-1 [High] 过宽的 IPC 命令面:任意可执行文件启动 + 任意路径读写/递归删除
- **类别**:授权 / 攻击面 / 权限提升
- **位置**:`commands.rs:5091`(`open_with_app`)、`:3730`(`docker_exec` 本地分支)、`:3303`/`:3350`/`:5019`(`read_local_text_file`/`local_write_text`/`local_remove_dir` 等);`tauri.conf.json:10`(`withGlobalTauri: true`);`capabilities/default.json`
- **问题**:这些自定义 `#[tauri::command]` 直接接收 webview 传入的路径/参数、以应用权限执行,**无路径白名单/scope/vault 门槛**,且绕过 Tauri `fs` capability 机制:
  - `open_with_app(file_path, app_path)` → `Command::new(&app_path).arg(&file_path).spawn()` = 任意可执行文件 + 任意参数 = **任意代码执行**。
  - `docker_exec` 本地分支可 `docker run -v /:/host …` 拿宿主机全权限。
  - `local_write_text` 覆写任意文件;`local_remove_dir` 递归删除(`validate_local_removable_dir` 仅拒根目录与相对路径,`/home/user` 仍可整树删);`read_local_text_file` 可读 `~/.ssh/id_rsa`、vault。
  - 配合 `withGlobalTauri`(任何脚本可 `window.__TAURI__.invoke`),一旦终端输出/AI 回复/文件名渲染路径出现 XSS,即升级为 RCE + 任意文件读写删。CSP `script-src 'self'` 提高了 XSS 门槛,故定 High。
- **修复**:`open_with_app` 改用系统"默认打开"语义或对 `app_path` 白名单;文件类命令限制在应用已知目录 / dialog 明确选择的路径(可用一次性 token);`local_remove_dir` 加强约束(禁 home 根、要求路径深度阈值);评估关闭 `withGlobalTauri` 改预加载桥接。

### TAURI-2 [Medium] 运行时 `std::env::set_var` 数据竞争(可致崩溃)(横切,见 SSH-6)
- **位置**:`commands.rs:503-509`(`set_proxy_env_var`)、`:511-531`,经 `save_network_proxy_config`(`:544`)/`clear_network_proxy_config`(`:555`)触发
- **问题**:运行时改 6 个代理环境变量,与 reqwest 构建 client 时读环境并发。glibc 上 `setenv` 重分配 `environ` 与并发 `getenv` 可段错误。启动时调用(`lib.rs:46`)安全,运行时命令路径不安全。
- **修复**:移除运行时 `set_var`;让 reqwest client 显式读内部代理状态并 `.proxy(...)`。

### TAURI-3 [Medium] 代理 URL(可含用户名/口令)明文落盘
- **类别**:凭据安全
- **位置**:`commands.rs:448-470`(`validate_network_proxy_url` 显式允许 userinfo)、`:494-501`(写 `network-proxy.json`)
- **问题**:`http://user:pass@host:port` 通过校验并明文写盘。主口令/host 密码/AI key/sync 凭据都进 keychain,代理凭据是唯一明文落盘的凭据。
- **修复**:剥离 userinfo,凭据部分存 keychain(复用 `save_sync_backend_credential`),文件只留 scheme/host/port。

### TAURI-4 [Medium] host-key 交互提示无超时,前端不响应会挂起握手/连接(横切,见 SSH-12)
- **位置**:`host_key.rs:56-87`(`rx.await.unwrap_or(HostKeyResponse::Reject)`)
- **问题**:等待前端 `respond_host_key` 期间若窗口关闭/崩溃/事件未达,只有 sender drop 才解除;`connect_timeout`(15s)不覆盖等待人工应答段,多次触发累积挂起连接。
- **修复**:`ask` 内用 `tokio::time::timeout` 包裹 `rx.await`,超时按 Reject 并清理 `pending_host_key`;窗口销毁时清空该 host 的 pending。

### TAURI-5 [Low] `TransferSink::rewind_bytes` 原子下溢回绕
- **位置**:`sftp/transfer.rs:419-430`
- **问题**:`fetch_sub` 返回旧值,旧值 < delta 时存储的 AtomicU64 回绕成接近 `u64::MAX`,`.saturating_sub` 只作用于返回的局部值不修正原子。多 worker 并发重试 rewind 时聚合进度可被写成天文数字(仅影响进度显示)。
- **修复**:改 `fetch_update` CAS 循环,或 `load` 后 `store(saturating_sub)`。

### TAURI-6 [Low] 锁毒化连锁 panic(横切,见 CORE-8)
- **位置**:全局大量 `state.app.lock().unwrap()` 等
- **修复**:关键状态锁改 `parking_lot` 或 `unwrap_or_else(|e| e.into_inner())`。

### TAURI-7 [Low] AI 会话历史明文落盘(横切,见 FE-5)
- **位置**:`commands.rs:817`(`write_ai_sessions_to_disk` → `ai-sessions.json`)
- **问题**:完整对话与 `command_results`(命令+输出)明文存 config 目录,可能含敏感命令输出。
- **修复**:纳入 vault 加密,或提供"不持久化/加密存储"选项。

### TAURI-8 [Low] async 命令中的阻塞 IO
- **位置**:`sftp/mod.rs:326`(`std::fs::metadata`)、`commands.rs:898`(`fs::read` 最大 16MiB)
- **修复**:改 `tokio::fs`。

### TAURI-9 [Low] 临时/下载目标的符号链接跟随(本地 TOCTOU)
- **位置**:`sftp/file.rs:448`(`File::create` 跟随符号链接)、`commands.rs:5063`、`sftp/file.rs:394`
- **修复**:创建暂存文件用 `create_new`(O_EXCL),拒绝已存在项。

### TAURI-10 [Low] `docker_exec`/`collect_system_metrics` 远程分支每次新建整条 SSH 会话
- **位置**:`commands.rs:3684`、`:3730` 远程分支 → `connect_host_sessions`
- **问题**:绕过 `sftp_pool`,每次轮询完整握手(含跳板)且用 Interactive 策略,host key 未知时可能每次弹窗。
- **修复**:复用连接池或走已建立的会话。

---

## F. 桌面前端(main.js / redact.js)

### FE-1 [High] 脱敏绕过:带空格的引号值只脱敏首词,其余明文外发 AI
- **类别**:redaction-bypass
- **位置**:`redact.js:172-173`(`KEY_VALUE_RE` 值捕获 `([^\s"';,]+)`)
- **问题**:值匹配在首个空白处停止。含空格的密码/口令(引号包裹常见)只有第一个词被替换。已用 Node 复现:`export DB_PASSWORD="correct horse battery staple"` → `"[REDACTED_SECRET] horse battery staple"`。UI 明确承诺"已在本地做基础脱敏后才发送"(`main.js:5972`),给用户**虚假安全感**。此类 `export VAR="多词口令"` 完全可能出现在终端快照(AI 主要输入源)。
- **修复**:引号包裹时匹配到闭合引号(`(["'])(...)\1`),或对已识别键名将其后到行尾/闭引号全部替换。

### FE-2 [Medium] 脱敏正则二次回溯 → 主线程冻结(ReDoS 式 DoS)
- **类别**:dos / redaction
- **位置**:`redact.js:172-173`(前缀 `[\w.-]*`);同步运行于主线程 `main.js:5970, 5996`
- **问题**:`[\w.-]*(?:password|...)` 在无关键词的长连续 `[\w.-]` 串上 O(n²) 回溯。实测 50k 字符 ≈ 2.6s、200k ≈ 42s。用户在 AI 输入框粘贴超长无换行文本,或 AI 回复含超长单行(大段 base64),下次请求整体重跑历史时造成秒级~数十秒 UI 冻结。(正常终端快照按行不触发。)
- **修复**:加输入长度上限或按行分块;前缀改有界 `[\w.-]{0,64}`;或移入 Web Worker。

### FE-3 [Medium] 每会话 Map 无清理,长时间运行内存泄漏
- **类别**:memory-leak
- **位置**:`aiConversationByPane`(`main.js:3359`)、`aiSessionIdentityByPane`(`:3360`)、`terminalSidePanelByPane`(`:3370`)、`aiRequestStateByPane`(`:3355`);`closeTab`(`:14976`)/`disconnectPaneSession`(`:14897`)未 `.delete()`
- **问题**:键 `session:${id}`/`pane:${id}` 单调生成永不重复,标签关闭时清了 term/observer/listener/timer 却不删这些 Map 条目,每条还挂整段对话,频繁开关标签无限增长。
- **修复**:在 `closeTab`/`disconnectPaneSession` 按 pane/session 键删除上述 Map 条目。

### FE-4 [Medium] AI 建议命令一键执行无二次确认,且存在提示注入面
- **类别**:ipc-misuse / prompt-injection
- **位置**:内联命令点击 `main.js:5139` → `executeAiCommand`(`:5510`,直接 `sendTextToPane(..., {submit:true})`);代码块"批准执行"`:6033`;带 `confirm()` 的 `runCommandInActiveTerminal`(`:5486`)已成死代码
- **问题**:AI 回复中的命令单击即在活动终端立即执行(自动回车),无预览确认。终端输出(部分由远端 SSH 服务器控制)会送入 AI,构成注入链:恶意远端输出 → 诱导 AI 给出恶意命令 → 用户一键执行。
- **修复**:对 AI 生成命令保留确认预览(尤其命令源头受远端影响时)。

### FE-5 [Low] AI 会话历史明文落盘(横切,见 TAURI-7)
- **位置**:`main.js:4942`(`persistCurrentAiSession`)→ 后端 `commands.rs:817`
- **问题**:`aiMessages`(用户输入原文 + AI 回复,均未脱敏)持久化为明文,绕过加密 vault 模型。
- **修复**:纳入 vault 加密,或持久化前脱敏。

### FE-6 [Low] AI Markdown 链接 href 无协议白名单(CSP 已缓解)
- **位置**:`main.js:5150`(`a.href = link?.[2] || "#"`)
- **问题**:AI 返回的 `[文本](url)` 直接赋 `href`,未限制 `javascript:`/`data:`。CSP `script-src 'self'` + `target="_blank"` 使实际风险很低。
- **修复**:仅允许 `http(s):`/`mailto:`,否则回退纯文本。

### FE-7 [Low] ESC 序列可拆分脱敏关键词/值(对终端快照已缓解)
- **位置**:`redact.js` 全部正则
- **问题**:`pass\x1b[1mword=abc` 不被脱敏,但终端快照走 `translateToString` 已剥离 ANSI,仅影响用户粘贴的含 ESC 文本。
- **修复**:脱敏前先剥离 ANSI 转义。

### FE-8 [Low] 缺少全局 `unhandledrejection` 处理
- **位置**:无 `window.addEventListener("unhandledrejection")`;`main.js:8911` 等个别 `.then()` 无 `.catch()`
- **修复**:加全局兜底避免静默失败。

---

## G. Android 客户端

### AND-1 [High] 生物识别仅为 UI 门禁,未与主密码密钥做加密绑定
- **类别**:认证绕过 / 凭据安全
- **位置**:`data/biometric/MasterPasswordStore.kt:16-25, 30-34`;`data/biometric/BiometricGate.kt:29-67`;`ui/unlock/UnlockScreen.kt:93-97, 250-252`
- **问题**:主密码(解锁整个 vault、可导出全部 SSH 私钥/密码/AI key)在勾选"记住"后明文存入 `EncryptedSharedPreferences`。`MasterKey.Builder` 只用 `AES256_GCM`,**未调 `setUserAuthenticationRequired(true)`**;`BiometricPrompt.authenticate()` **未传 `CryptoObject`**,且仅要求 `BIOMETRIC_WEAK`。故生物识别只是界面门禁——密钥在任何时刻(无需指纹)都可解密。能以应用身份执行代码(重打包/无障碍/root)者即可绕过指纹调 `load()` 拿主密码。
- **修复**:用 `setUserAuthenticationRequired(true)` 创建受 Keystore 用户认证保护的密钥加解密缓存的主密码;`authenticate()` 传绑定该密钥的 `CryptoObject`;认证等级提升到 `BIOMETRIC_STRONG`。

### AND-2 [Medium] 敏感界面未设 `FLAG_SECURE`,可被截屏/录屏/最近任务缩略图捕获
- **类别**:信息泄露
- **位置**:`MainActivity.kt`(全局无 `FLAG_SECURE`);受影响 `UnlockScreen`、`HostEditScreen.kt:361-384`、`AiScreen.kt:1489-1500`、`SyncScreen`、`TerminalScreen`
- **问题**:全项目无任何 `FLAG_SECURE`。系统截屏、录屏、后台"最近任务"缩略图会捕获主密码、私钥、API key、终端会话内容。
- **修复**:承载敏感内容的窗口设 `FLAG_SECURE`(可做成设置项)。

### AND-3 [Medium] Terminal 等 FFI 对象从不显式 `destroy()`,原生内存长期滞留
- **类别**:FFI 资源生命周期
- **位置**:`data/SessionManager.kt:158`(创建 `Terminal(cols, rows, 10_000u)`);移除处 `:154, 176, 191-196, 249-252, 262-264` 仅从 map 移除,从不 `terminal.destroy()`
- **问题**:uniffi 的 `Terminal` 是需显式释放对象(持 1 万行滚动缓冲的 Rust 内存),全 App 无任何 `.destroy()`/`.close()`。会话关闭/切换后仅丢 Kotlin 引用,原生缓冲要等 Cleaner GC 后才回收,频繁开关累积泄漏。
- **修复**:从 `sessions` 移除 `ActiveSession` 时确定性调 `terminal.destroy()`(或 `use{}`)。

### AND-4 [Medium] Release 构建在缺签名环境变量时回退 debug 签名密钥
- **类别**:构建 / 供应链
- **位置**:`app/build.gradle.kts:33-67`
- **问题**:release `signingConfig` 仅当四个 keystore 环境变量齐备才用正式 keystore,否则回退 `signingConfigs.getByName("debug")`。在未配置的机器上 `assembleRelease` 会产出用**公开 debug 密钥**签名的"release"包;因 Android 更新强制同签名密钥,他人可用同一 debug key 构造并覆盖安装恶意更新。(与 CI `release.yml` 中 Android debug keystore 回退互相印证。)
- **修复**:检测不到 release keystore 时让 release 构建直接失败,而非静默回退 debug。

### AND-5 [Low] 应用内 APK 更新缺少显式完整性校验
- **位置**:`data/UpdateChecker.kt:68-157`
- **问题**:HTTPS 下载 APK 后直接经 FileProvider + `ACTION_VIEW` 安装,App 自身不校验哈希/签名,仅依赖 TLS 与系统"同签名密钥"校验;叠加 AND-4 则防线失效。
- **修复**:在 release 资产提供并校验 SHA-256/签名指纹后再安装。

### AND-6 [Low] AutoSyncController 在 release 仍向 Logcat 输出同步 profile 名/错误
- **位置**:`data/AutoSyncController.kt:134, 138, 155`;`build.gradle.kts:56`(`isMinifyEnabled=false` 无日志剥离)
- **问题**:`Log.d/w` 打印 `auto-sync ok: ${p.name}` 与失败原因,release 同样执行;虽不含凭据,但把用户命名的同步配置与后端错误写入 logcat。
- **修复**:release 用 `BuildConfig.DEBUG` 包裹或 ProGuard `-assumenosideeffects` 剥离 `Log.d/v`。

### AND-7 [Low] 导出的 MainActivity 接受任意应用发送的自定义 action
- **位置**:`AndroidManifest.xml:30-40`(`exported="true"`)、`MainActivity.kt:44-54`
- **问题**:MainActivity 因 LAUNCHER 必须导出;`handleIntent` 处理 `ACTION_OPEN_ACTIVE_SESSION`,任意第三方 App 可发送触发导航到活动会话终端(当前无敏感数据,危害有限)。
- **修复**:改带包名校验/自定义 permission,或在 `handleIntent` 校验来源。

### AND-8 [Low] 主线程执行同步 FFI 与 EncryptedSharedPreferences 解密(ANR 隐患)
- **位置**:`ui/unlock/UnlockViewModel.kt:39, 44, 51, 58`
- **问题**:`vaultStatus()`(同步 JNI)、`hasCachedPassword()`/`load()`(Tink 解密)在主线程 `refreshStatus()`/`init` 执行。当前开销小,设备慢/vault 大时有 jank/ANR。(`unlock`/`create` 已正确用 `Dispatchers.Default`。)
- **修复**:移到 `Dispatchers.IO/Default` 后回写 UI。

### AND-INFO 设计提示:AI 会把终端视口/选区内容发送到第三方 AI 端点
- **位置**:`TerminalScreen.kt:486`(`contextProvider = { sessions.viewportText() }`)→ `AiScreen.kt:604/659`
- **说明**:属功能设计,但终端内容可能含密钥/令牌/内网信息,会外发到 OpenAI/兼容端点。建议 UI 明确提示并提供发送前脱敏/确认(且注意此处依赖 FE-1 的脱敏,同样受其绕过影响)。

---

## 已验证稳健的部分(供排除,非缺陷)

- **密码学原语**:XChaCha20-Poly1305 用 24 字节随机 nonce(无计数器/固定 nonce、无复用),每记录 `HKDF(master, salt=record_id)` 独立密钥,AAD 绑定 `vault_id|record_id|kind|revision`;compaction 搬运快照是复用同一密文而非重新加密,不构成 nonce 复用。HKDF 域分隔清晰(vault `RECORD_INFO` vs sync `SYNC_RECORD_INFO`/`SYNC_KEYRING_INFO`)。常量时间比较正确。Argon2id 默认 64 MiB/3/4 超过 OWASP 阈值,每 vault 16 字节随机盐。
- **`.ztlog` 编解码**(`event.rs`):`checked_add` + 边界校验,截断/多余尾字节/未知 magic/未来 schema 全部映射 `Corrupt`,测试充分。
- **SQL**:全部 `params![]` 参数化,无拼接注入;`WITHOUT ROWID`;迁移带 `user_version` 且拒绝更高 schema。
- **路径遍历防护**:SFTP/WebDAV 对后端返回列表项调 `validate_repo_relative_path`(拒 `..`/绝对路径/反斜杠/`\0`),WebDAV 在 percent-decode 后校验;desktop 侧 `validate_remote_leaf_name`/`normalize_remote_path`/`is_local_path_within`(基于 canonicalize)覆盖目录树复制/删除,有单测。
- **日志脱敏**:`ConnectConfig` 手写 `Debug` 脱敏,`method_name` 仅记类型;全仓无密码/私钥/API key 进 `tracing`。
- **host key 校验**:普通密钥按自描述 key blob 判定,Strict 对 Unknown/Mismatch 一律拒绝,提示丢失默认拒绝;主机证书按 CA 签名、有效期、host principal/type、critical options 与吊销状态严格校验,未知证书不做叶子 TOFU;ProxyJump 在 CLI 与 sync 路径每跳独立校验,信任未盲目传递;转发默认绑 `127.0.0.1`。
- **前端 XSS**:约 60 处 `innerHTML` 的动态数据均经 `escapeMetricText` 转义或走 `textContent`/DOM API;CSP `script-src 'self'` 无 `unsafe-inline`/`unsafe-eval`;`localStorage` 仅存非敏感项;WebLinks 经 `plugin:opener`、`window.open` 带 `noopener`;xterm/监听器/observer/timer 在 pane 释放时清理;密码显示需二次验证。
- **FFI 边界**:uniffi 0.28 对同步调用与 async future 均 `catch_unwind`,工作区/Tauri 未设 `panic=abort`,panic 不 unwind 越过 C ABI(无 UB);async 导出函数不跨 await 持 std Mutex(编译器 Send 约束排除)。
- **Android 基线**:`allowBackup=false`、`usesCleartextTraffic=false` + `network_security_config`(base-config 禁明文)、密码输入框 `PasswordVisualTransformation`、`FileProvider`/`Service`/`Provider` 均 `exported=false`、docker 命令 shell 引号转义、无 WebView/JavascriptInterface。
- **同步(sync)**:Lamport 时钟本体原子 CAS 正确(`clock.rs`);async 无跨 await 持锁死锁(`EngineState` 为 tokio mutex)。

---

## 优先修复路线图

**P0 — 直接违背产品核心承诺(数据完整性 / E2E 加密 / MITM 防护),尽快修**
1. SYNC-1 + SYNC-2 + SYNC-3 + SYNC-4:落地文末「统一合并规范」,给记录加 `(last_clock, server_rev, parent_revision)` 因果元数据,apply 按因果比较、`mark_clean` 用 CAS、tombstone 带 clock、并发对称记冲突。这一处数据模型改动同时消解四条 Critical/High。
2. SYNC-5 + SYNC-6:`write_new` 临时文件+rename 崩溃原子 + 单事件错误隔离;manifest 条件写(CAS)+ compact 顺序调整。
3. CORE-1:冲突 payload 加密后再落盘。
4. SSH-1:RSA host key 类型归一化 / 仅以 blob 判定。
5. TAURI-1:收敛 `open_with_app`/`docker_exec`/`local_*` 的输入约束与路径白名单。
6. FE-1:修复脱敏引号值截断;AND-1:生物识别加密绑定。

**P1 — 显著削弱防护姿态**
- SSH-2(哈希主机名/@revoked)、CORE-2(回滚防护)、TAURI-2/SSH-6(`env::set_var` 竞争)、TAURI-3(代理凭据入 keychain)、FE-2(脱敏 ReDoS)、FE-4(AI 命令确认)、AND-2(FLAG_SECURE)、AND-4(签名回退)、DEP-1/DEP-3(升级 russh + CI 加 `cargo audit`/测试门禁)。

**P2 — 纵深防御与健壮性**
- 横切:CORE-3/SSH-3(zeroize)、CORE-8/SSH-9/TAURI-6(锁毒化)、CORE-9/SSH-14(脱敏 Debug)、TAURI-7/FE-5(AI 会话加密)。
- 其余 Low 项按迭代排期。

### 附:同步统一合并规范(修 SYNC-1/3/4/7 的单一杠杆)
记录模型持久化 `(last_clock, server_rev, parent_revision)`;`apply_event` 用可交换合并函数,不再读 `dirty`:
1. **幂等**:`E.revision == L.server_rev` → no-op
2. **因果祖先(旧)**:E 在 L 血缘链上且 clock 更小 → **丢弃 E**(合法覆盖,非冲突;delete 同理,旧于当前态的 delete 被丢弃 → 无复活)
3. **快进**:`E.parent_revision == L.server_rev` → **采纳 E**
4. **并发(不可比)**:`E.parent_revision != L.server_rev` 且互不支配 → **冲突**,用 `(lamport_clock, device_id)` **确定性**裁决存活值 + 在**每个副本对称**记录胜负双方 payload

两条硬约束:裁决必须是双写 `(clock, device_id)` 的确定性函数(不依赖本地到达顺序,否则先采纳方无法收敛);冲突登记必须对称(被覆盖侧原值始终可从本地冲突日志恢复)。delete-vs-update 并发进入第 4 类,建议"并发更新优先于删除"(可配置)。H5/H6(SYNC-5/6)、SYNC-2、SYNC-8 属正交轴,需各自修。

---

*本报告由多 agent 并行审计生成,所有 `文件:行号` 基于审计时的 main 分支源码。修复后建议对每条 P0/P1 补充回归测试并纳入 CI。*
