# Rust build artifacts
target/
**/*.rs.bk
*.pdb

# Cargo lockfile is kept for the workspace root only (it's already in core/)
# Keep core/Cargo.lock; ignore lockfiles for any nested example crates.

# Editor / OS noise
.vscode/
.idea/
.DS_Store
Thumbs.db
.claude/
.codex/

# Local env
.env
.env.local

# Per-user known_hosts created during testing, if placed in repo root
test-known-hosts

# Tauri / desktop
desktop/src-tauri/gen/
desktop/node_modules/
desktop/dist/

# Android
android/.gradle/
android/local.properties
android/**/build/
android/app/src/main/jniLibs/
# uniffi bindings under app are build copies; source of truth is core/
android/app/src/main/java/com/zeroterm/ffi/

# Keep vendored crates (alacritty_terminal Android patch)

